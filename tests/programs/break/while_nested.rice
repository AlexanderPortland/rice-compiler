fn main() {
  let acc = 0 in
  let x = 0 in
  while x < 10 {
    let y = 0 in
    while y < 10 {
      acc := acc + (x * y);
      y := y + 1
    };
    x := x + 1
  };
  println(int_to_string(acc))
}